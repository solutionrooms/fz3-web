# dependencies
node_modules/

# build output
dist/
dist-web/

# generated AssetLibrary (regenerated by `npm run assets` from the SWF)
/public/assets/

# intermediates (regenerable from the compressed SWF)
/flaming-zombooka-3.uncompressed.swf
/abc_bytecode.bin

# vendored decompiler (JPEXS FFDec — download separately, ~43 MB)
/tools/ffdec/
/tools/ffdec.zip
/tools/ffdec_export.log

# oracle: generated harness SWFs (rebuilt by `npm run oracle:build`)
/tools/oracle/build/

# FFDec media exports — regenerable from the SWF (the .as scripts + binaryData XML are kept as the
# porting reference; the bulky media is not)
/extracted/shapes/
/extracted/sounds/
/extracted/images/
/extracted/fonts/
/extracted/morphshapes/
/extracted/texts/

# render spike (throwaway proof-of-concept — heavy)
/spike/node_modules/
/spike/public/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/
